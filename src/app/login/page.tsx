import { currentUser } from "@/lib/auth/session"; import { redirect } from "next/navigation"; import { login } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser())redirect("/dashboard");const {error}=await searchParams;return <main className="login panel"><h1>SSL Monitor</h1><p className="muted">Authorized agency staff only.</p>{error&&<p className="error">{error}</p>}<form action={login} className="grid"><label className="full">Email<input name="email" type="email" autoComplete="username" required/></label><label className="full">Password<input name="password" type="password" autoComplete="current-password" required/></label><button className="full">Sign in</button></form></main>}
