import { config } from "@/lib/config";import { csrfToken } from "@/lib/auth/csrf";import { testSmtp } from "./actions";
const notices:Record<string,string>={sent:"Test email sent successfully.","missing-recipient":"Enter a test recipient.","authentication-failed":"SMTP authentication failed. Verify the mailbox credentials and that Authenticated SMTP is enabled for this mailbox.","delivery-failed":"The SMTP server could not deliver the test message. Check the server logs and SMTP configuration."};
export default async function Settings({searchParams}:{searchParams:Promise<{smtp?:string}>}){const c=config();const {smtp}=await searchParams;const configured=c.EMAIL_PROVIDER==="resend"?Boolean(c.RESEND_API_KEY&&c.RESEND_FROM):Boolean(c.SMTP_HOST&&c.SMTP_FROM);return <><h1>Settings</h1>{smtp&&notices[smtp]&&<p className={smtp==="sent"?"panel":"error"}>{notices[smtp]}</p>}<section className="grid"><div className="panel"><h2>Application</h2><p>Name: {c.APP_NAME}</p><p>Base URL: {c.APP_BASE_URL}</p><p>Timezone: {c.MONITORING_TIMEZONE}</p><p>Daily monitoring: {c.DAILY_MONITORING_ENABLED?`Enabled at ${c.DAILY_MONITORING_TIME}`:"Disabled"}</p><p>Stale after: {c.MONITORING_STALE_HOURS} hours without a completed run</p><p>Retention: {c.HISTORY_RETENTION_DAYS} days</p><p>Default thresholds: {c.DEFAULT_ALERT_THRESHOLDS}</p></div><div className="panel"><h2>Email</h2><p>Provider: {c.EMAIL_PROVIDER==="resend"?"Resend API":"SMTP"}</p><p>Status: {configured?"Configured":"Not configured"}</p><p>Default recipients: {c.DEFAULT_ALERT_RECIPIENTS||"None"}</p><form action={testSmtp}><input type="hidden" name="csrf" value={await csrfToken()}/><label>Test recipient<input name="recipient" type="email" required/></label><br/><button>Send test notification</button></form><p className="muted">Email-provider secrets are environment-only and are never displayed.</p></div></section></>}
